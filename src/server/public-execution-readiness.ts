// Offline checkpoint: source-level hard stop, not an operator/environment feature flag.
// Revisit only through the new public-execution plan and reviewed acceptance evidence.
export const PUBLIC_EXECUTION_IMPLEMENTATION_READY = false;
