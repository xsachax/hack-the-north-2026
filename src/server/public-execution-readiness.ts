// Hosted-validation candidate. Operator opt-in and per-session native attestation remain mandatory.
// This enables the implemented path, not a claim that every public site is supported.
export const PUBLIC_EXECUTION_IMPLEMENTATION_READY = true;
